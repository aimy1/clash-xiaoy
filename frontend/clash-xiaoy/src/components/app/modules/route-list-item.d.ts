import { SvgIconComponent } from '@mui/icons-material';
export declare const RouteListItem: ({ name, path, icon, onlyIcon, }: {
    name: string;
    path: string;
    icon: SvgIconComponent;
    onlyIcon?: boolean;
}) => import("@emotion/react/jsx-runtime").JSX.Element;
export default RouteListItem;
